"use client";

import MuiCachedIcon from "@mui/icons-material/Cached";
import { SvgIconOwnProps } from "@mui/material/SvgIcon";

export const ShuffleIcon = (props: SvgIconOwnProps) => <MuiCachedIcon {...props} />;
