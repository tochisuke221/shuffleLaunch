"use client";

import MuiSearchIcon from "@mui/icons-material/Search";
import { SvgIconOwnProps } from "@mui/material/SvgIcon";

export const SearchIcon = (props: SvgIconOwnProps) => (
  <MuiSearchIcon {...props} />
);
