'use client'

import MuiExpandMoreIcon from "@mui/icons-material/ExpandMore";
import { SvgIconOwnProps } from '@mui/material/SvgIcon'

export const ExpandMoreIcon = (props: SvgIconOwnProps) => (
  <MuiExpandMoreIcon {...props} />
);
