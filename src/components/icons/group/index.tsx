"use client";

import MuiGridIcon from "@mui/icons-material/GridViewRounded";
import { SvgIconOwnProps } from "@mui/material/SvgIcon";

export const GroupIcon = (props: SvgIconOwnProps) => (
  <MuiGridIcon {...props} />
);
