"use client";

import MuiGroupAddIcon from "@mui/icons-material/GroupAdd";
import { SvgIconOwnProps } from "@mui/material/SvgIcon";

export const AttendanceAddIcon = (props: SvgIconOwnProps) => (
  <MuiGroupAddIcon {...props} />
);
